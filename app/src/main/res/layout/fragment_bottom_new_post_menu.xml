<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/add_post_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="?attr/colorSurface"
    tools:context="com.cliche.app.ui.posts.NewPostFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Image preview -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="240dp"
            android:layout_marginTop="12dp"
            android:background="?android:attr/colorBackground"
            android:foregroundGravity="center">

            <ImageView
                android:id="@+id/add_post_image_preview"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerInside"
                android:contentDescription="@string/add_post_pick_cta"
                android:visibility="gone"
                android:clickable="true"
                android:focusable="true" />

            <LinearLayout
                android:id="@+id/add_post_pick_container"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:gravity="center">

                <ImageButton
                    android:id="@+id/add_post_pick_image"
                    android:layout_width="56dp"
                    android:layout_height="56dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/add_post_pick_image"
                    android:scaleType="fitCenter"
                    android:src="@drawable/ic_menu_camera_24dp"
                    android:tint="?attr/colorTertiary"/>

                <TextView
                    android:id="@+id/add_post_pick_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/add_post_pick_cta"
                    android:layout_marginTop="8dp" />
            </LinearLayout>
        </FrameLayout>

        <!-- Caption -->
        <EditText
            android:id="@+id/add_post_caption"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:hint="@string/add_post_hint_caption"
            android:inputType="textMultiLine"
            android:minLines="3"
            android:maxLines="6"
            android:autofillHints="none" />

        <!-- Location / tags row (simple placeholders) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="8dp">

            <Button
                android:id="@+id/add_post_location"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                style="?android:attr/buttonBarButtonStyle"
                android:text="@string/add_post_add_location" />

            <Button
                android:id="@+id/add_post_tags"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                style="?android:attr/buttonBarButtonStyle"
                android:text="@string/add_post_tag_people" />
        </LinearLayout>

        <!-- Post button -->
        <Button
            android:id="@+id/add_post_send"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="@string/add_post_share" />

    </LinearLayout>
</ScrollView>
